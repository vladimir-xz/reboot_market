{% extends 'base.html.twig' %}

{% block title %}Edit shipping{% endblock %}

{% block body %}
<div class="w-1/2 mx-auto">
    <h1>Edit shipping</h1>

    {{ include('admin/shipping/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('shipping.index') }}">back to list</a>

    {{ include('admin/shipping/_delete_form.html.twig') }}

    <div class="my-4">
        <a href="{{ path('admin') }}">Back to admin lobby</a>
    </div>
</div>
{% endblock %}
