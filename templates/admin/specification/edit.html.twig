{% extends 'base.html.twig' %}

{% block title %}Edit Specification{% endblock %}

{% block body %}
<div class="w-1/2 mx-auto">
    <h1>Edit Specification</h1>

    {{ include('admin/specification/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('specification.index') }}">back to list</a>

    {{ include('admin/specification/_delete_form.html.twig') }}

    <div class="my-4">
        <a href="{{ path('admin') }}">Back to admin lobby</a>
    </div>
</div>
{% endblock %}
