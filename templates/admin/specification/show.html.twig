{% extends 'base.html.twig' %}

{% block title %}Specification{% endblock %}

{% block body %}
    <h1>Specification</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ specification.id }}</td>
            </tr>
            <tr>
                <th>Property</th>
                <td>{{ specification.property }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ specification.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('specification.index') }}">back to list</a>

    <a href="{{ path('specification.edit', {'id': specification.id}) }}">edit</a>

    {{ include('admin/specification/_delete_form.html.twig') }}

    <div class="my-4">
        <a href="{{ path('admin') }}">Back to admin lobby</a>
    </div>
{% endblock %}
