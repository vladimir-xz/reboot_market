{% extends 'base.html.twig' %}

{% block title %}New Specification{% endblock %}

{% block body %}
    <h1>Create new Specification</h1>

    {{ include('admin/specification/_form.html.twig') }}

    <a href="{{ path('specification.index') }}">back to list</a>

    <div class="my-4">
        <a href="{{ path('admin') }}">Back to admin lobby</a>
    </div>
{% endblock %}
